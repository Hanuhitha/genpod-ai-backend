version: '3'
services:
  genpod:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
      - "7689:7689"
    volumes:
      - .:/opt/genpod
    command: sleep infinity
